<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>See <%= table %></title>
</head>
<body>
    <h1> See the <%= table %> datas</h1>
    
    <!-- <% if item.length > 0 { %> -->
    <table>
        <tr>
            <% for (var i=0; i < Object.getOwnPropertyNames(item[0]).length; i++){ %>
                <th> <%= Object.getOwnPropertyNames(item[0])[i] %> </th>
            <%}%>
        </tr>
        <% for (var i=0; i < item.length ; i++){ %>
            <tr> 
                <% for (var j=0; j < Object.getOwnPropertyNames(item[0]).length; j++){ %>  
                    <% var a = Object.getOwnPropertyNames(item[0])[j] %>
                    <td> <%= item[i][a]%></td>
                <%}%>
            </tr>
        <% } %>
    </table>
    <!--<% } %> -->
    <!-- <% else { %>
        <h2> No data in the database</h2>
    <% } %>
    -->
</body>
</html>